meta {
  name: auth/login
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/auth/login
  body: json
  auth: none
}

body:json {
  { 
      "email" : "louis.le_bec@centrale-med.fr",
      "password" : "secret"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
