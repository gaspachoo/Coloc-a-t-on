meta {
  name: users/:id/profile-photo del
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/users/:id/profile-photo
  body: multipartForm
  auth: none
}

params:path {
  id: 1
}

body:multipart-form {
  photo: @file(D:\Gaspard\Pictures\image.jpg)
}

vars:pre-request {
  id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
